package GUI;

import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {

        MyFrame frame = new MyFrame();

        /*
        JFrame frame = new JFrame();//creates a frame
        frame.setVisible(true); //make frame visible
        frame.setSize(1500, 800); //sets the x-dimension, and y-dimension of frame
        frame.setTitle("GUI JFrame Test");  //sets title of frame
        frame.setResizable(true); //prevent frame from being resized
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit from application
        ImageIcon image = new ImageIcon("logo.png"); //create an ImageIcon
        frame.setIconImage(image.getImage()); //change icon of frame
        frame.getContentPane().setBackground(Color.lightGray); //change color of background

         */



    }
}

